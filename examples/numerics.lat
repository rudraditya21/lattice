// Numeric tower examples: ints, floats, complex, decimal, rational
i = 1 + 2
f = 1.5 + 2.5
c = complex(1, 2) * complex(0, 1)   // (-2 + i)
d = decimal(1.25) + decimal(0.75)
r = rational(1, 3) + rational(1, 6)
mixed = cast(f64, decimal(2.0)) + 1.0  // explicit cast to mix with float

print(i)
print(f)
print(c)
print(d)
print(r)
print(mixed)
